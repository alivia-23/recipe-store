{
  "name": "recipe-store",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'exam2' (`git checkout -b exam2`)\r * modify the `questions.md` file to have the answers required\r * Add any files necessary to create the application below\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the reviewer(s)\r * Due by 11:59pm (PT) Sun Mar 29",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./node_modules/pm2/bin/pm2 start server.js --name recipe-store",
    "stop": "node ./node_modules/pm2/bin/pm2 stop recipe-store",
    "build": "npx webpack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "uuid": "^7.0.2",
    "pm2": "^4.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.0",
    "babel-loader": "^8.1.0",
    "nodemon": "^2.0.2",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11"
  }
}
